package commands

import "github.com/jtarchie/pocketci/executor"

// Execute is the `ci execute` command. It runs a pipeline locally using the
// given orchestrator driver.
type Execute = executor.Execute
