# fly.toml app configuration file generated for ci on 2026-02-25T20:02:05-05:00
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#

app = 'ci'
primary_region = 'sjc'

[build]
  dockerfile = 'Dockerfile.fly'

[deploy]
  strategy = 'rolling'

[[mounts]]
  source = 'ci_data'
  destination = '/data'

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = 'off'
  auto_start_machines = true

[[vm]]
  memory = '512mb'
  cpu_kind = 'shared'
  cpus = 1
  memory_mb = 512
